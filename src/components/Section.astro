---
interface Props {
  id: string;
  title: string;
}
const { id, title } = Astro.props;
---

<section id={id} class="mx-auto max-w-3xl scroll-mt-20 px-6 py-14">
  <h2 class="mb-6 text-sm font-semibold uppercase tracking-widest text-[var(--highlight)]">
    {title}
  </h2>
  <slot />
</section>
